# 整理版验证记录

验证日期：2026年9月20日。执行方：AI整理助手。学习者此前的原练习截图与本次整理版自动验证是两组独立证据。

## MySQL环境

使用本机MySQL Server 8.4.11程序启动独立临时实例，数据目录位于本次工作目录，未连接原有MySQL服务。关闭TCP网络和MySQL X插件，仅使用唯一命名的本地共享内存通道。临时实例采用仅供测试的初始化账号，项目未保存密码或连接配置，验证完成后实例已停止。

## 实际结果

- 01_schema.sql：在新实例中创建演示库和5张表，成功。
- 02_seed.sql：插入模拟数据并提交，成功；行数4、3、3、2、2。
- 03_inventory_report.sql：四行完整字段与README预期逐项一致，库存95、20、0、0。
- 04_customer_return_check.sql：返回0行。
- 05_boundary_tests.sql：只返回“累计退货超量”，出库30、累计退货35、超量5。
- 06_learning_comparisons.sql：全部五条查询结果符合注释，错误关联算法返回300/100。
- 累计退货回归：在临时事务中给出库编号1增加26支退货，原有5支，累计31支；检查返回剩余可退数量−1，随后回滚。
- 约束反例：重复商品主键报1062；入库关联不存在商品报1452；出库数量0报3819。反例均未保留。
- 测试结束前重新核对完整库存报表，结果仍为95、20、0、0。

以上执行直接读取交付目录中的六份SQL文件，没有为了通过MySQL测试而改写SQL。另运行项目内Python标准库检查程序，13项内存检查全部通过；该程序只用于补充逻辑回归。

## 限制

没有测试远程服务器、不同MySQL版本或SQL模式、并发、性能、大数据量、权限体系、部署和ERP软件集成。当前结果只能支持这份模拟数据和列出的测试场景。学习者亲自复跑与口头解释仍待完成，不应在简历中宣称已独立实施或上线。
